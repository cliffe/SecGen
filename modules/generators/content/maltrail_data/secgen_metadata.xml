<?xml version="1.0"?>
<generator xmlns="http://www.github/cliffe/SecGen/generator"
           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xsi:schemaLocation="http://www.github/cliffe/SecGen/generator">
    <name>MalTrail Randomized Data Generator</name>
    <author>Rosie Fletcher</author>
    <module_license>MIT</module_license>
    <description>Generates randomized MalTrail threat events and custom trails for security training scenarios. Creates synthetic IPs, domains, URLs, and malware indicators. Output includes a flag event that embeds a CTF flag within the threat data.</description>

    <type>maltrail_data</type>
    <type>local_calculation</type>
    <platform>linux</platform>

    <read_fact>data_volume</read_fact>
    <read_fact>embedded_flag</read_fact>

    <default_input into="data_volume">
        <value>medium</value>
    </default_input>

    <default_input into="embedded_flag">
        <generator type="flag_generator"/>
    </default_input>

    <output_type>generated_strings</output_type>

</generator>